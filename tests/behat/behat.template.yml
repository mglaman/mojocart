imports:
    - behat.common.yml

default:
  extensions:
    Behat\MinkExtension:
      base_url: http://${drupal.uri}
    Drupal\DrupalExtension:
      drush:
        root: ${drush.root}
      drupal:
        drupal_root: '/path/to/drupal/root'
